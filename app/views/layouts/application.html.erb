<!DOCTYPE html>
<html>
  <head>
    <title>Volatile CRM</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="turbo-prefetch" content="false">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gray-600">
    <div class="flex flex-col min-h-[920px]">
      <div class="bg-gray-700 h-6">
        <div class="flex justify-between">
          <div class="flex items-stretch h-6 opacity-0 hover:opacity-100">
            <%= link_to render("icons/home"), root_path, class: "flex items-center text-gray-400 text-sm px-2 hover:bg-gray-500 hover:text-white" %>
            <%= link_to "A", alerts_path, class: "flex items-center text-gray-400 text-sm px-2 hover:bg-gray-500 hover:text-white" %>
            <%= link_to "L", page_layouts_path, class: "flex items-center text-gray-400 text-sm px-2 hover:bg-gray-500 hover:text-white" %>
            <%= link_to "C", contacts_path, class: "flex items-center text-gray-400 text-sm px-2 hover:bg-gray-500 hover:text-white" %>
            <%= link_to "B", charts_path, class: "flex items-center text-gray-400 text-sm px-2 hover:bg-gray-500 hover:text-white" %>
            <%= link_to "T", tabs_path, class: "flex items-center text-gray-400 text-sm px-2 hover:bg-gray-500 hover:text-white" %>

            <% if params[:controller] == "tabs" %>
              <%= link_to "←", tab_path(id: @prev_id), class: "flex items-center text-gray-400 text-sm px-2 hover:bg-gray-500 hover:text-white" if @prev_id.present? %>
              <%= link_to "→", tab_path(id: @next_id), class: "flex items-center text-gray-400 text-sm px-2 hover:bg-gray-500 hover:text-white" if @next_id.present? %>
              <%= tag.span(@id.titleize, class: "flex items-center text-red-400 text-sm px-2") %>
            <% end %>
          </div>

          <div class="flex items-stretch h-6 opacity-0 hover:opacity-100">
            <%= link_to "Preview", "/rails/view_components", target: "_blank", class: "flex items-center text-gray-400 text-xs px-2 hover:bg-gray-500 hover:text-white" %>
            <%= link_to "Lookbook", "/lookbook", target: "_blank", class: "flex items-center text-gray-400 text-xs px-2 hover:bg-gray-500 hover:text-white" %>
          </div>
        </div>
      </div>

      <div class="w-full max-w-6xl mx-auto px-4 pt-4">
        <div class="text-sm text-white font-semibold mb-4">
          <%= link_to "Volatile CRM", root_path %>
        </div>

        <%= yield %>
      </div>
    </div>
  </body>
</html>
