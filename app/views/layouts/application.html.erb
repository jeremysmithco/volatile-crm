<!DOCTYPE html>
<html>
  <head>
    <title>Volatile CRM</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="turbo-prefetch" content="false">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <div class="flex flex-col h-screen">
      <% if params[:controller] == "tabs" %>
        <div class="flex bg-gray-700 opacity-0 hover:opacity-100">
          <%= link_to "←", tab_path(id: @prev_id), class: "text-white text-sm px-2 py-1 hover:bg-gray-500" if @prev_id.present? %>
          <%= link_to "→", tab_path(id: @next_id), class: "text-white text-sm px-2 py-1 hover:bg-gray-500" if @next_id.present? %>
          <%= tag.span(@id.titleize, class: "text-white text-sm px-2 py-1") %>
        </div>
      <% end %>

      <div class="w-full max-w-6xl mx-auto px-4 pt-6">
        <div class="text-lg text-gray-600 font-semibold mb-4">Volatile CRM</div>

        <%= yield %>
      </div>
    </div>
  </body>
</html>
